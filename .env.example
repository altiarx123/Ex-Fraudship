# Application runtime
SAFE_MODE=true
DATA_BACKEND=sqlite
SMS_PROVIDER=mock

# Backend
FRONTEND_URL=http://localhost:3000

# Optional mysql
MYSQL_ROOT_PASSWORD=example
SAFE_MODE=true
DATA_BACKEND=sqlite
SMS_PROVIDER=mock
# SMTP settings (if using email_gateway)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@gmail.com
# SMTP_PASS=app-password
# TELEGRAM_BOT_TOKEN=your_bot_token
